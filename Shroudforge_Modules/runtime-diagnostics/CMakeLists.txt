cmake_minimum_required(VERSION 3.24)
project(shroudforge-runtime-diagnostics LANGUAGES CXX)
if(NOT WIN32 OR NOT CMAKE_SIZEOF_VOID_P EQUAL 8)
    message(FATAL_ERROR "Runtime diagnostics require Windows x64")
endif()
foreach(tool dump-live-components inspect-component-metadata live-entity-manager-sample live-entity-managers live-type-references)
    add_executable(${tool} native-tools/${tool}.cpp)
    target_compile_features(${tool} PRIVATE cxx_std_20)
    set_property(TARGET ${tool} PROPERTY MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<CONFIG:Debug>:Debug>")
endforeach()
